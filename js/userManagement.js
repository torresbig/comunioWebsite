/**
 * Verwaltet die User-Auswahl und Spieleranzeige
 * @module userManagement
 */

/**
 * Initialisiert das User-Dropdown
 * @param {Array} usersData - Array mit allen User-Daten
 */
// file retired: functionality kept inline in useruebersicht.html
// Placeholder to avoid accidental imports. Convert to ES module if desired.